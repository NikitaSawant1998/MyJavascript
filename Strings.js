let str="hello world"
let f=[1,2,3]
console.log(str.split(" ")); // Converts string to array
console.log(f.join()) //Converts array to string
console.log(str.replace(" ","@")) // Replaces part of a string
console.log(str.substring(3)) // Extracts part of a string
console.log(str.trim())
